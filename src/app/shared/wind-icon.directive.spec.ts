import { WindIconDirective } from './wind-icon.directive';

describe('WindIconDirective', () => {
  it('should create an instance', () => {
    const directive = new WindIconDirective();
    expect(directive).toBeTruthy();
  });
});
